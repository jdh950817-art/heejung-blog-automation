// ===== App Config (pre-configured) =====
// Supabase credentials are baked in — no need to enter in settings
const APP_CONFIG = {
  supabaseUrl: '{{SUPABASE_URL}}',
  supabaseKey: '{{SUPABASE_KEY}}',
  storageBucket: 'blog-photo',
  geminiKey: '{{GEMINI_KEY}}',
};
